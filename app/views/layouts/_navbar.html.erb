<nav class="fixed-top section navbar navbar-expand-lg navbar-light bg-cream fancy-font">
  <div class="navbar-header">
    <a class="navbar-brand" href="/"> Jennifer's Design Studio </a>
  </div>
  <button class="navbar-toggler ml-auto" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li><%= link_to "Cart", shopping_cart_path(current_cart), class: "nav-link" %></li>
      <% if !logged_in? %>
        <li> <%= link_to "Login", login_path, class:"nav-link"%>
      <% else %>
        <li><%= link_to "Logout", logout_path, method: :delete, class: "nav-link" %></li>
      <% end %>
    </ul>
  </div>
</nav>

