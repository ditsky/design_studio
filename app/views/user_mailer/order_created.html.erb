

<p> Hi Mom! </p>

<p>
You have recieved an order! Below are the details:
@cards.size = <%= @cards.size %>
</p>

<table class="table table-striped">
    <thead >
        <h3 class="center pad-top pad-bottom">Customer Info:</h3>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Email</th>
            <th scope="col">Phone Number</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td class="no-wrap"> <%= @name %> </td>
            <td class="no-wrap"> <%= @email %> </td>
            <td class="no-wrap container"> <%= @address.phone_number%> </td>
        </tr>
    </tbody>
    <thead>
        <h3 class="center pad-top pad-bottom">Address:</h3>
        <tr>
            <th scope="col">Address Line 1</th>
            <th scope="col">Address Line 2</th>
            <th scope="col">City</th>
            <th scope="col">State</th>
            <th scope="col">Zip</th>
    </thead>
        <tbody>
            <tr>
                <td class="no-wrap container"> <%= @address.address_line_1%> </td>
                <td class="no-wrap container"> <%= @address.address_line_2%> </td>
                <td class="no-wrap container"> <%= @address.city%> </td>
                <td class="no-wrap container"> <%= @address.state%> </td>
                <td class="no-wrap container"> <%= @address.zip%> </td>
            </tr>
        </tbody>
    <thead >
        <h3 class="center pad-top pad-bottom">Purchased Items</h3>
        <tr>
            <th scope="col">Description</th>
            <th scope="col">Quantity</th>
            <th scope="col">Price</th>
            <th> </th>
        </tr>
    </thead>
    <tbody>
        <% @cards.each do |card| %>
            <tr>
                <td class="no-wrap"> <%= card.short_description %> </td>
                <td class="no-wrap container"> 
                    <div class="row">
                    <%= @sizes[card.id] %> 
                    </div>
                </td>
                <td class="no-wrap"> $<%= price_to_string(@sizes[card.id] * card.price) %> </td>
            </tr>
        <% end %>
    </tbody>
</table>

<p> 
    Click this link to view all the orders on the site <%= link_to "click me", "https://jennifers-design-studio.herokuapp.com/orders"%>.
    Make sure to make sure the user has not canceled the order before you start working on it,
    and change the order status to "in process" when designing it and "shipping" once you have shipped it.
</p>

