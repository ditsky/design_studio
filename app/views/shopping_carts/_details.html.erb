<h3 class="pad-bottom"> Order Summary </h3>
<div class="card border-dark mb-3 bg-pink">
    <div class="card-body">
        <h5 class="card-title">Subtotal <span style="float:right;"> $<%= @total %> </span> </h5>
        <h5 class="card-title">Shipping <span style="float:right;"> FREE </span> </h5>
        <hr>
        <h4 class="card-title">Total <span style="float:right;"> $<%= @total %> </span> </h4>
        <br>
        <% if logged_in? && !@empty%>
            <%= button_to "Checkout", new_order_path, method: :get, class: "btn btn-sm btn-magenta center"%>
        <% elsif !@empty%>
            <%= link_to "Login", login_path, method: :get, class: "btn btn-sm btn-primary center"%>
            <%= button_to "Guest Checkout", new_order_path, method: :get, class: "btn btn-sm btn-magenta center margin-top-sm"%>
        <% else%>
            <p> You Must Have items in your cart to checkout </p>
            <%= button_to "Checkout", new_order_path, :disabled => true, method: :get, class: "btn btn-sm btn-magenta center"%>
        <% end %>
    </div>
</div>